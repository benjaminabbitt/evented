#YAML 1.2
---
queryHandler:
  url: evented-query-handler.default.svc.cluster.local:30002

transport:
  type: rabbitmq
  rabbitmq:
    url: rabbitmq.default.svc
    exchange: evented
    queue: UNKNOWN

evented:
  url: business.url

database:
  type: mongodb
    url: mongodb://mongodb.default.svc.cluster.local:27017
    database: projection

domain: domain