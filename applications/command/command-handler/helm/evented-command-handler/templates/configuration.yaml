apiVersion: v1
kind: ConfigMap
metadata:
  name: config-command-handler
data:
  keys: {{- tpl (toYaml .Values.command_handler.configuration) . | indent 2}}

---

apiVersion: v1
kind: ConfigMap
metadata:
  name: config-business-logic
data:
  keys: {{- tpl (toYaml .Values.business_logic.configuration) . | indent 2}}

