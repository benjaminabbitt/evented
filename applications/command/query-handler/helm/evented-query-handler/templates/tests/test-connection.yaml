apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "evented-query-handler.fullname" . }}-test-connection"
  labels:
    {{- include "evented-query-handler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "evented-query-handler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
