#YAML 1.2
---
otherCommandHandlers:
  - domain: foo
    url: foo.url
  - domain: bar
    url: bar.url

queryHandler:
  url: queryhandler.url

transport:
  kind: rabbitmq
  rabbitmq:
    url: amqp://guest:guest@rabbitmq.default.svc:5672
    exchange: exchange
    queue: queue

evented:
  url: business.url

database:
  url: database.url
  name: name

domain: domain

saga:
  url: sample-saga.url

port: 1748
