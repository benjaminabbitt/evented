apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    evented: grpc-projector-coordinator
  name: grpc-projector-coordinator
spec:
  selector:
    matchLabels:
      evented: grpc-projector-coordinator
  template:
    metadata:
      labels:
        evented: grpc-projector-coordinator
    spec:
      containers:
        - name: evented-grpc-placeholder-projector-coordinator
          image: evented-grpc-projector-coordinator
          imagePullPolicy: Never
          env:
            - name: CONFIG_MGMT_TYPE
              value: consul
            - name: CONSUL_HOST
              value: consul-headless.default.svc.cluster.local:8500
            - name: CONSUL_CONFIG_TYPE
              value: yaml
            - name: CONSUL_KEY
              value: grpc-placeholder-projector-coordinator
            - name: APP_NAME
              value: grpc-placeholder-projector-coordinator
---
apiVersion: v1
kind: Service
metadata:
  labels:
    evented: grpc-projector-coordinator
  name: evented-grpc-projector-coordinator-exported
spec:
  type: NodePort
  ports:
    - port: 1748
      name: 1748-tcp
      nodePort: 30749
    - port: 40000
      name: 40000-tcp
      nodePort: 30810
  selector:
    evented: grpc-projector-coordinator

