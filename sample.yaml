#YAML 1.2
---
otherCommandHandlers:
  - domain: foo
    url: foo.url
  - domain: bar
    url: bar.url

queryHandler:
  url: queryhandler.url

transport:
  kind: rabbitmq
  rabbitmq:
    url: transport.url
    exchange: exchange
    queue: queue

evented:
  url: business.url

database:
  kind: mongodb
  mongodb:
    url: database.url
    name: name
    collection: collection

domain: domain

saga:
  url: sample-saga.url

port: 1313
