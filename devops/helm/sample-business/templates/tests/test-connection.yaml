apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-business.fullname" . }}-test-connection"
  labels:
    {{- include "sample-business.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-business.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
