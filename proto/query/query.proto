syntax = "proto3";
package evented.query;

import "core/evented.proto";


service EventQuery {
    rpc GetEventBook (Query) returns (evented.core.EventBook) {
    }
}

message Query {
    string domain = 1;
    string root = 2;
    uint32 lowerBound = 3;
    uint32 upperBound = 4;
}