syntax = "proto3";
package evented.business;
option go_package = "business;evented_business";

import "core/evented.proto";

service BusinessLogic {
    rpc Handle (evented.core.ContextualCommand) returns (evented.core.EventBook) {
    }
}